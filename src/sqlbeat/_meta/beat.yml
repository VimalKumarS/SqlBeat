################### Sqlbeat Configuration Example #########################

############################# Sqlbeat ######################################

sqlbeat:
  # Defines how often an event is sent to the output
  period: 1s
